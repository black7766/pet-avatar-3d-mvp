# 项目记忆

## 项目入口

- 本地独立项目目录：`D:\work\pet-avatar-3d-mvp`
- 当前主页面：`poc_output/paimomo_compare.html`
- 当前完整服务：`petavatar_server.py`
- 当前生成主流程：`poc.py`
- 当前提示词集中维护：`prompts.py`
- 早期本地 WebP MVP：`legacy_webp_mvp/`

## 当前决策

- 不再把这套 Demo 散落在 `D:\work\ai_tools` 多个目录里，新项目目录作为后续交接和投资人 Demo 的统一入口。
- 火山方舟 API Key 不进入 git。使用 `.env` 或环境变量 `ARK_API_KEY`。
- Seedance 调用必须保持无声：`generate_audio=false`。
- 展示资产优先使用透明 WebP/WebM，页面内预览，不提供媒体下载入口。
- 当前展示方案属于“视频资产 + 状态机”的 2.5D 方案，不是最终 skeleton/IK 实时 3D 方案。
- 2026-06-23 更新：上传原图必须同时保存到 `inputs/` 和对应 `poc_output/<pet_id>/uploaded_original.*`。历史卡片优先读取输出目录内副本，避免迁移项目后源图丢失。
- 2026-07-10 更新：展示资产收敛为 `idle` 静息、`fast_walk` 快走、`sleep` 睡眠。快走合并走动/奔跑表现，三者都直接生成稳定循环态，不生成状态过渡。
- 2026-07-10 更新：每种风格先生成快走/睡眠独立状态首帧，再生成 4 秒 Seedance 首尾帧循环；本地采用内存并行自适应绿幕抠图。
- 2026-07-10 A/B：视频 tokens 降 19.8%，加入主体边缘低饱和绿幕反光修正后，本地抠图墙钟仍降 58.0%，平均循环缝从 0.991% 降到 0.296%；由于新增 4 张状态图，刊例总成本基本持平。
- 2026-07-10 480p 探针：萌宠快走单段从 87,300 降到 38,800 tokens，按六段投影单宠约 ¥3.36，较旧链路低 41.3%；细毛略软且本次出现完整转身，因此只能作为需抽检的经济档。720p 修正版仍有正面到 3/4 视角漂移，但已消除完整 360° 转圈。

## 方案脉络

1. 先用 Seedream 把用户宠物照片标准化为实体版和萌宠版。
2. 用 Seedream 生成快走和睡眠目标状态首帧。
3. 用 Seedance 生成 4 秒无声目标状态循环视频并锁定同状态首尾帧。
4. 本地一次解码、并行自适应绿幕抠图，处理毛发边缘与轮廓附近低饱和绿幕反光，并保护高饱和绿色身份细节。
5. 合成透明 WebP/WebM。
6. 页面通过状态按钮和历史卡片展示原图、实体版、萌宠版。
7. 当前动作资产只保留 `idle/fast_walk/sleep`；都以稳定循环资产为主，过渡动画不是当前默认规格。

## 待办

- 把 `prompts.py` 里的终端乱码注释清理成标准 UTF-8 中文，避免后续协作阅读困难。
- 基于《猫狗四大状态与静息动作池设计表.xlsx》实现静息动作池权重、冷却和采样安全标记。
- 基于当前四状态生成更多宠物样例，并补充自动 QA 结果。
- 增加自动 QA：首尾帧差异、主体可见面积、绿边残留、文件体积、帧数。
